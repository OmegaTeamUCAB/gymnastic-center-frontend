class Comment {
    String id;
    String content;
    String userId;
    DateTime createdAt;

    Comment({
        required this.id,
        required this.content,
        required this.userId,
        required this.createdAt,
    });
}